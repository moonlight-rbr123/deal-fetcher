# Environment Variables (Optional)

# Server Configuration
PORT=3000

# Frontend Configuration (if needed for server-side rendering)
# API_URL=http://localhost:3000

# Note: The frontend now supports runtime configuration via:
# 1. URL Parameter: ?api=https://deal-fetcher.railway.internal
# 2. Browser Settings: ⚙️ API Settings button in the app
# 3. Browser Console: localStorage.setItem('dealFetcherApi', 'https://your-backend.com')
